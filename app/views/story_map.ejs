<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8"/><meta charset="UTF-8">
    <title>
      <% if (title) { %>
        <%= title %>
      <% } else { %>
        LittleSis
      <% } %>
    </title>
    <% if (title) { %>
      <meta property="og:title" content="<%= title %>">
    <% } %>
    <% if (image) { %>
      <meta property="og:image" content="<%= image %>">
    <% } %>
    <% if (description) { %>
      <meta property="og:description" content="<%= description %>">
    <% } %>
    <% if (content) { %>
      <link href="<%= env == 'development' ? 'http://localhost:8090' : '/react' %>/main.css" rel="stylesheet"/>
      <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css" rel="stylesheet" />
    <% } %>
  </head>

  <body>
    <div id="content">
      <%- content %>
    </div>
    <% if (content) { %> 
      <script>
        var svg = document.getElementById('svg');
        svg.setAttribute("xmlns:xlink", "http://www.w3.org/1999/xlink");
        var height = document.getElementById("mainContainer").clientHeight - 120;
        svg.setAttribute("height", height);
      </script>
    <% } %>
    <% if (env == 'development') { %>
      <script src="http://localhost:8090/webpack-dev-server.js"></script>
      <script src="http://localhost:8090/build/vendors.js"></script>
      <script src="http://localhost:8090/build/app.js"></script>
    <% } else { %>
      <script src="/react/app.js"></script>
    <% } %>

    <% if (ga_id) { %>
      <script>
        (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
        m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
        })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

        ga('create', '<%= ga_id %>', 'auto');
        ga('send', 'pageview');
      </script>
    <% } %>
  </body>
</html>
